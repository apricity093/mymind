# 项目开发规范与协作指南

---

## 一、变更记录规范

每次对本项目文件进行改动后，**必须**在项目根目录的 `report.md` 中记录本次改动。

- 若 `report.md` 不存在，应先新建该文件。
- 记录内容至少包括：
  - 改动日期
  - 改动文件
  - 改动摘要
  - 是否触及冻结清单

---

## 二、测试与运行环境

- 需要运行测试时，请使用虚拟环境 `learn_claude`。
- 除非明确说明，否则默认修改 Python 版本 `mymind` 的内容。

---

## 三、程序设计原则

1. **权限与校验**
   - 本项目非安全攻防类项目，你有权进行合理的校验。
   - 禁止过度防御，避免为极端罕见或不可能出现的 case 编写冗余防御代码。

2. **评价标准（Rubric）设计**
   - 需要设定评分标准的地方，应保持合理灵活，避免过度机械化。

---

## 四、思考方式

- 所有设计与实现应遵循**第一性原理**，从问题本质出发推导解决方案，而非依赖经验或惯性思维。

---

## 五、协作约定

- 本文件作为团队协作的基础规范，所有成员应共同遵守。
- 如有调整需求，请提出修改建议并同步更新本文档。
- git commit在HISTORY.md（若不存在则新建）文档中按照格式（标题 = commit 内容 + 时间，随后简要描述功能）然后推送到远程仓库：   https://github.com/apricity093/mymind.git；仅HISTORY.md发生变动不必再单独commit、push

---

> **版本**：v1.0  
> **最后更新**：2026-08-08