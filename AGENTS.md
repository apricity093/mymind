每次对本项目进行文件改动后，都必须在项目根目录的 `report.md` 中记录本次改动（使用中文）；如果 `report.md` 不存在，应先新建。记录内容至少包括：改动日期、改动文件、改动摘要、是否触及冻结清单。



需要运行测试的话使用虚拟环境learn_claude;

除非明确说明，否则默认修改python版本mymind的内容

思考要遵循第一性原理

## 程序编写要求

1. 我们不是一篇安全攻防项目，你有权力进行校验，但是禁止过度防御
2. 禁止反复的基本不可能出现的case写防御
3. 需要rubric的地方不要过度机械化